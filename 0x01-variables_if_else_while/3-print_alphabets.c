#include <stdio.h>
/**
 * main- print upper and lowercase alphabets
 * Return: Always 0 (success)
*/
int main(void)
{
	char alp;
	char alph;

	for (alp = 'a'; alp <= 'z'; alp++)
		putchar(alp);
	for (alph ='A'; alph <= 'Z' alph++)
		putchar(alph);
	putchar('\n');
	return (0);
}
